// use conditional shorthand

// bad practice
if(isValid === true) {
    //do something
}
if(isValid === false) {
    //do something
}

// good practices
if(isValid) {
    //do something
}
if(!isValid) {
    //do something
}